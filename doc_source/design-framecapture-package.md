# Organize encodes in a Frame Capture output group<a name="design-framecapture-package"></a>

A frame capture output group can contain only one video JPEG encode\. Put that single encode in one output\.